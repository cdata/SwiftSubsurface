import XCTest
@testable import SwiftSubsurface

final class SwiftSubsurfaceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(Subsurface().text, "Hello, World!")
        XCTAssertNotNil(SubsurfaceView())
    }
}
